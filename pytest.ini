[pytest]
testpaths = tests
pythonpath = .
addopts =
    -ra
    --strict-markers
    --strict-config
markers =
    unit: fast, in-process tests with no external services
    integration: tests that require the docker-compose stack
    manual: tests run by hand; never executed in CI
