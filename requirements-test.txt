# Test dependencies. Pulls in runtime so a test venv is self-contained.
-r requirements.txt


# --- Static analysis ---
isort==8.0.1
black==26.3.1
flake8==7.3.0
mypy==2.1.0
bandit==1.9.4
vulture==2.16

# --- Testing ---
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
coverage-badge==1.1.2
respx==0.23.1
# coverage-badge imports pkg_resources, which was removed from setuptools
# in 81.0.0. Until coverage-badge migrates to importlib.metadata, pin
# setuptools below that cutoff so the badge target works on fresh venvs.
setuptools<80
