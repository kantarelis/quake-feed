// Placeholder — the Leaflet map + live SSE alerts land in Task 6.
export function MapView() {
  return (
    <section>
      <h1 className="text-2xl font-semibold tracking-tight">Live map</h1>
      <p className="mt-1 text-slate-500">Leaflet map with live alerts — coming in Task 6.</p>
    </section>
  );
}
