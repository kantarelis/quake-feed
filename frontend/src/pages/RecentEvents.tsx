// Placeholder — the /events/recent timeline lands in Task 4.
export function RecentEvents() {
  return (
    <section>
      <h1 className="text-2xl font-semibold tracking-tight">Recent events</h1>
      <p className="mt-1 text-slate-500">Newest earthquakes — coming in Task 4.</p>
    </section>
  );
}
