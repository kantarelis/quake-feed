// Placeholder — the /alerts/filters config form lands in Task 5.
export function AlertConfig() {
  return (
    <section>
      <h1 className="text-2xl font-semibold tracking-tight">Alert filters</h1>
      <p className="mt-1 text-slate-500">Create and manage alert filters — coming in Task 5.</p>
    </section>
  );
}
